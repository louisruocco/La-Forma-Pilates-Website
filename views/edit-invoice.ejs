<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <link rel="stylesheet" href="/css/styles.css">
    <title>La Forma Pilates | Invoice no. <%= invoice.id %></title>
</head>
<body>
    <div class="nav">
        <nav>
            <ul>
                <li><a href="/admin/client/<%= invoice[0].name %>/<%= invoice[0].surname %>">Back to Client Dashboard</a></li>
            </ul>
        </nav>
    </div>
    <div class="admin-title">
       <h1> Invoice no. <%= invoice.id %></h1>
    </div>
    <div class="input-section">
        <form action="/auth/admin/<%= invoice[0].name %>/<%= invoice[0].surname %>/<%= invoice[0].id %>/edit-invoice" method = "POST">
            <div class="date-section">
                <span class = "input-group-text">Date</span>
                <input class = "form-control" type="text" name = "date"  value="<%= invoice[0].date %>">
            </div>
            <div class="time-section">
                <span class = "input-group-text">Duration of Session</span>
                <input class = "form-control" type="text" name = "time" value="<%= invoice[0].time %>">
            </div>
            <div class="cost-section">
                <span class = "input-group-text">Amount</span>
                <input class = "form-control" type="text" name = "amount" value="<%= invoice[0].amount %>">
            </div>
            <div class="status-section">
                <span class = "input-group-text">Status</span>
                <input class = "form-control" type="text" name = "status" value="<%= invoice[0].status %>">
            </div>
            <button onClick = "history.back()">Save Changes</button>
        </form>
    </div>
</body>
</html>